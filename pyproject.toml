[project]
name = "llm-cache-hw"
version = "0.1.0"
description = "ДЗ 2.4: кеширование LLM"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "redis>=5.0.0",
]

[project.scripts]
demo = "demo:main"
